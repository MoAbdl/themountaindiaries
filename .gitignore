# Raw OpenTopoMap tiles, kept only so re-running scripts/make-route-map.py
# costs the tile server nothing. The committed artefact is the stitched
# assets/images/map-<slug>.jpg, not the tiles.
.tilecache/
__pycache__/
*.pyc

# macOS Finder droppings
.DS_Store
